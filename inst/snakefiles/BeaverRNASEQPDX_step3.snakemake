#configfile: "config/config.yaml"

SIDs = config["SIDs"]
species = config["species"]
chrs = config["chrs"]
# 运行所有流程时，rule all 需要加入所有的output汇总
rule all:
  input:
    expand(os.path.join(config["bsmapDir"],"Filtered_bams" ,"{sample}_fixed_"+config["graft"]+"_Filtered.bam"), sample=SIDs) ,
    expand(os.path.join(config["outDir_mCall"], "{sample}_"+config["graft"]+".txt"), sample=SIDs)

# build matrix

include:"rules/rnaseq_htseq_xen.smk"

